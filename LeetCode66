class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        string = ''
        for i in digits:
            string += str(i)
        num = int(string)
        num += 1
        my_list = [int(x) for x in str(num)]
        return my_list
